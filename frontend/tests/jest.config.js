// - Run `npm test` to run the tests

module.exports = {
    testMatch: ['**/frontend/test/**/*.js', '**/?(*.)+(spec|test).js'],
    testEnvironment: 'node', // For testing in a browser-like environment
  };
